# Video Splitter - Нарізка відео

Простий додаток для нарізки відеофайлів на сегменти з двома режимами роботи.

## 🚀 Встановлення

1. Встановіть залежності:
```bash
pip install -r requirements.txt
```

2. Переконайтеся, що FFmpeg встановлено в системі:
```bash
brew install ffmpeg  # для macOS
```

## 📱 Використання

### Запуск додатку
```bash
python main.py
```

### Інтерфейс
1. **Оберіть відеофайл**:
   - Перетягніть файл у вікно програми
   - Або натисніть кнопку "📂 Обрати файл"

2. **Налаштуйте параметри**:
   - **Тривалість сегменту**: вкажіть кількість секунд (1-3600)
   - **Режим нарізки**: оберіть один з двох режимів

3. **Натисніть "🚀 START"** для початку нарізки

## ⚙️ Режими роботи

### ⚡ Швидкий режим (без перекодування)
- **Швидкість**: Дуже швидко
- **Якість**: Оригінальна
- **Формат**: Такий самий як вхідний файл
- **Недолік**: Ріже тільки по ключових кадрах (можлива неточність по часу)

**FFmpeg команда**:
```bash
ffmpeg -i input.mp4 -c copy -map 0 -segment_time 60 -f segment output_%03d.mp4
```

### 🎯 Точний режим (з перекодуванням)
- **Швидкість**: Повільніше
- **Якість**: Перекодується в H.264/AAC
- **Формат**: Завжди MP4
- **Перевага**: Ріже точно по вказаному часу

**FFmpeg команда**:
```bash
ffmpeg -i input.mp4 -c:v libx264 -c:a aac -preset fast -segment_time 60 -f segment -reset_timestamps 1 output_%03d.mp4
```

## 📂 Структура вихідних файлів

Програма створює папку з назвою відеофайлу в тій же директорії, де знаходиться оригінальний файл:

```
/path/to/video/
├── original_video.mp4
└── original_video/
    ├── original_video_001.mp4
    ├── original_video_002.mp4
    ├── original_video_003.mp4
    └── ...
```

## 🔧 Системні вимоги

- **Python 3.7+**
- **PySide6**
- **FFmpeg** (має бути доступний в системі)
- **macOS/Windows/Linux**

## 💡 Поради для Apple Silicon

На M1/M2 Mac FFmpeg працює дуже швидко з H.264 через CPU. Якщо потрібна максимальна швидкість у точному режимі, можна змінити preset на `ultrafast` в коді.

## 🐛 Вирішення проблем

1. **"FFmpeg не знайдено"**:
   - Встановіть FFmpeg: `brew install ffmpeg` (macOS)
   - Додайте FFmpeg в PATH

2. **"Помилка доступу до файлу"**:
   - Переконайтеся, що є права на запис в папку з відеофайлом

3. **Повільна робота в точному режимі**:
   - Це нормально - перекодування вимагає часу
   - Використовуйте швидкий режим, якщо точність по часу не критична

## 📝 Ліцензія

MIT License
